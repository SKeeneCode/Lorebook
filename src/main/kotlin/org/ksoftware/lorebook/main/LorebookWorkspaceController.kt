package org.ksoftware.lorebook.main

import tornadofx.*

/**
 * Controller for the lorebook workspace. This controller is stateless and should always be injected from the same scope
 * so that it remains a singleton.
 */
class LorebookWorkspaceController : Controller() {

}